export const KEY = 'active';

export const ACTIVE_ACCOUNT_ACTION = `${KEY}/activeAccount`;

export const ACTIVE_ACCOUNT_SUCCESS = `${KEY}/activeAccountSuccess`;

export const ACTION_BEGIN = `${KEY}/actionBegin`;

export const ACTION_END = `${KEY}/actionEnd`;
